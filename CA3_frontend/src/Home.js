import React from "react"


    const Home = () => {
        return (
           <div>
               <p>
                   Message
                   This startcode can be used for Reactjs webapps. It contains a login
                   class component as security. The backend intended for this is in 
                   another git repository, but should be used in conjunction with this
                   frontend code.
               </p>
           </div>
        );
    };
    
    export default Home;